//! Content IDs — re-exported from bd_core::ids for convenience.
//!
//! bd_data depends on bd_core, so ContentId lives in bd_core and is
//! re-exported here for external crate convenience.

pub use bd_core::ids::*;
